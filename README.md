# Buy-Videos-with-Crypto-website

1. try to launch every .py via powershell to see if every import is installed, watch video should say no pub key
2. install ffmpeg from github you need to have C:\ffmpeg\bin\ffmpeg.exe 
3. launch xampp apache and copy index etc in C:\xampp\htdocs
